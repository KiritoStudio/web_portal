# Copy to home-portal.env and fill in the real password. publish.sh installs that file on
# the target as /etc/home-portal.env, mode 600, readable only by root.
CONNECTION_STRING=Server=192.168.1.1;Port=5432;Database=web_portal;Username=db;Password=password
